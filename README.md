# Description
This project is based on the concept of ReAct. The LLM is powered with web search tool (DuckDuckGo). The model used is deepseek-chat. If the model cannot find an answer to the query, it will use the web search tool instead of hallucinating.
